## 1. Introduction

SalamandraTA is a specialized, state-of-the-art family of large language models (LLMs) explicitly engineered and fine-tuned for advanced translation tasks and translation-related workflows. Developed by the Barcelona Supercomputing Center (BSC) as part of the open-source Salamandra ecosystem, this model is designed to excel in handling multilingual data, particularly within the Iberian linguistic context, while showcasing high efficiency. Unlike general-purpose instruct models, SalamandraTA possesses deep architectural optimizations that allow it to process parallel corpora effectively, making it highly adept at not only text translation but also sophisticated downstream tasks such as automatic post-editing, paragraph-level translation, named entity recognition (NER), and grammar correction.

To accommodate different computational environments and hardware capabilities, the SalamandraTA family is distributed in various optimized parameter sizes, most notably the highly agile **2B (2 billion parameters)** version and the more robust **7B (7 billion parameters)** variant. The smaller models, such as SalamandraTA-2b-instruct, are meticulously compressed and quantized, making them ideal for running locally on standard consumer hardware. Meanwhile, the larger scales provide enhanced reasoning and context retention for complex, enterprise-level translation pipelines integrated into computer-assisted translation (CAT) tools.

* [salamandraTA-2b-instruct](https://huggingface.co/BSC-LT/salamandraTA-2b-instruct)
* [salamandraTA-7b-instruct](https://huggingface.co/BSC-LT/salamandraTA-7b-instruct)

To learn more about SalamandraTA read:

Javier Garcia Gilabert, Xixian Liao, Severino Da Dalt, Ella Bohman, Audrey Mash, Francesca De Luca Fornaciari, Irene Baucells, Joan Llop, Miguel Claramunt, Carlos Escolano, and Maite Melero. 2025. [From SALAMANDRA to SALAMANDRATA: BSC Submission for WMT25 General Machine Translation Shared Task](https://aclanthology.org/2025.wmt-1.37/). In Proceedings of the Tenth Conference on Machine Translation, pages 614–637, Suzhou, China. Association for Computational Linguistics.

## 2. Testing SalamandraTA in a Pyhton script

Before running the following script, you should install the following requirements:

```
torch
transformers
accelerate
```

This is a basic script to test SalamandraTA:

```
import torch
from transformers import AutoTokenizer, AutoModelForCausalLM

def translate_salamandra_manual(text, model_id, source_lang, target_lang):
    tokenizer = AutoTokenizer.from_pretrained(model_id)
    if tokenizer.pad_token is None:
        tokenizer.pad_token = tokenizer.eos_token

    model = AutoModelForCausalLM.from_pretrained(
        model_id,
        device_map="auto",
        dtype=torch.bfloat16
    )

    # 1. Manual construction of the prompt following the model card format
    # We define a System Prompt to provide context to the model
    system_prompt = "You are a professional translator."
    user_prompt = f"Translate the following text from {source_lang} into {target_lang}.\n{source_lang}: {text} \n{target_lang}:"

    # We assemble the parts using the <|im_start|> and <|im_end|> delimiters
    full_prompt = (
        f"<|im_start|>system\n{system_prompt}<|im_end|>\n"
        f"<|im_start|>user\n{user_prompt}<|im_end|>\n"
        f"<|im_start|>assistant\n"
    )

    # 2. Tokenization
    inputs = tokenizer(full_prompt, return_tensors="pt", add_special_tokens=False).to(model.device)
    input_length = inputs.input_ids.shape[1]

    # 3. Generation
    outputs = model.generate(
        inputs.input_ids,
        attention_mask=inputs.attention_mask,
        max_new_tokens=400,
        num_beams=5,
        early_stopping=True,
        pad_token_id=tokenizer.pad_token_id,
        eos_token_id=tokenizer.convert_tokens_to_ids("<|im_end|>") # We force it to know when to stop
    )

    # 4. Decoding
    result = tokenizer.decode(outputs[0][input_length:], skip_special_tokens=True)
    
    # We clean up any possible remnants of the final delimiter if present
    return result.replace("<|im_end|>", "").strip()

if __name__ == "__main__":
    #model_id = "BSC-LT/salamandraTA-2b-instruct"
    model_id = "../../models/salamandraTA-2b-instruct"
    # Usage example
    src = "English"
    tgt = "Spanish"
    frase = "Artificial intelligence (AI) is the capability of computational systems to perform tasks typically associated with human intelligence, such as learning, reasoning, problem-solving, perception, and decision-making."

    traduccio = translate_salamandra_manual(frase, model_id, src, tgt)
    print(f"\nTranslation: {traduccio}")
```

The script produces the following output:

```
Loading weights: 100%|█████████████████████████████████| 219/219 [00:00<00:00, 23951.83it/s]

Translation: La inteligencia artificial (IA) es la capacidad de los sistemas computacionales para realizar tareas típicamente asociadas con la inteligencia humana, tales como el aprendizaje, el razonamiento, la resolución de problemas, la percepción y la toma de decisiones.
```


## 3. Starting SalamandraTA in MTUOC-server

The easiest way is to use MTUOC-server-manager and download the recipe recipe-SalamandraTA.yaml. This would download the 2b model and the needed configuration files for the pair English-Spanish, but you can edit them to adapt to your specific needs.

<img width="928" height="666" alt="image" src="https://github.com/user-attachments/assets/28b5ead6-ea66-45c5-81ea-e58eaf3580c8" />

This would download the following files:

**config-server-SalamandraTA.yaml**

```
MTengine: HuggingFace
#download, Marian, Eole, OpusMT, NLLB, HuggingFace, Ollama, GoogleT, DeepL
system_name: SalamandraTA
model_config: ./salamandraTA-2b-instruct/config-model-SalamandraTA.yaml

MTUOCServer:
  port: 8000
  type: MTUOC
  #one of MTUOC, Moses, ModernMT, OpenNMT, NMTWizard
  verbosity_level: 1
  #specific configuration when acting as ONMT server
  log_file: log.log
  timeout: 45

dopreprocess: False
preprocess_config: None
dotokenization: False
tokenization_config: None
dopostprocess: False
postprocess_config: None

TranslationMemory: 
  use: False
  memo: None  
  minsim: 100
```

And inside the model's folder:

**config-model-SalamandraTA.yaml**

```
prompt_settings:
  template: "Translate the following text from English into Spanish.\nEnglish: {SLsegment}\nSpanish:"
  json_key: None
  regex_pattern: None   

model_settings:
  name: "./salamandraTA-2b-instruct"
  device: "cpu"
  #cpu or cuda
  trust_remote_code: true

generation_params:
  max_new_tokens: 512
  temperature: 0.0
  repetition_penalty: 1.2
  top_k: 40
  top_p: 0.9
  no_repeat_ngram_size: 3
  num_beams: 1
  use_eos_token: true
  stop_sequences: ["<|im_end|>", "\nEnglish:", "###"]
```

Where we can adapt the prompt to our language pair, use cpu or gpu, etc.


